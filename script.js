const games=[{title:'Edge Rush',description:'Click the edge before the ball breaks free.',icon:'🟢',tag:'REFLEX',url:'edge-rush.html'},{title:'Bubble Pop',description:'Clear the board before time runs out.',icon:'🫧',tag:'PUZZLE'},{title:'Garden Quest',description:'A tiny adventure with a big heart.',icon:'🌱',tag:'ADVENTURE'}];const grid=document.querySelector('#game-grid'),empty=document.querySelector('#empty');function render(q=''){const r=games.filter(g=>(g.title+' '+g.description+' '+g.tag).toLowerCase().includes(q.toLowerCase()));grid.innerHTML=r.map(g=>`<article class="game-card"><div class="game-art">${g.icon}</div><div class="game-info"><p class="eyebrow">${g.tag}</p><h3>${g.title}</h3><p>${g.description}</p>${g.url?`<a class="play" href="${g.url}">Play now →</a>`:`<a class="play" href="#" onclick="event.preventDefault();alert('Coming soon.');">Play now →</a>`}</div></article>`).join('');empty.hidden=r.length>0}document.querySelector('#search').addEventListener('input',e=>render(e.target.value));render();
